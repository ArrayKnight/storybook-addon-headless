import React, { ReactElement } from 'react'

import { Loader } from '.'

export default {
    title: 'Loader',
}

export const LoaderStory = (): ReactElement => <Loader />

LoaderStory.storyName = 'Loader'
